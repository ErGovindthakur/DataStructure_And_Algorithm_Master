// exploring Ternary and Nested ternary operator


// It's a simple ternary example
let checkTernary = 12 > 13 ? "True" : "False";
console.log(checkTernary);

// Here is nested ternary operator example
let num = 9;
console.log(num > 0 ? "Positive": num < 0 ? "Negative": "Zero");